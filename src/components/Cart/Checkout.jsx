import React from 'react';

const Checkout = () => {
	return (
		<button
			id="checkout-btn"
			name="checkout-btn"
			type="button"
			className="checkout-button"
		>
			Checkout
		</button>
	);
};

export default Checkout;
